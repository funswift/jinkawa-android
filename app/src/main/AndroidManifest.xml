<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taross.jinkawa_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

<application
        android:name="com.nifty.cloud.mb.core.NCMBApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:windowSoftInputMode="stateAlwaysHidden">

        <!-- プッシュ通知タップ時に起動するActivityの設定 -->
        <meta-data android:name="openPushStartActivity" android:value=".MainActivity"/>

        <!-- 通知エリアに表示されるアイコンの設定 -->
        <meta-data android:name="smallIcon" android:resource="@drawable/common_google_signin_btn_icon_dark"/>

        <!-- 通知エリアに表示されるアイコンカラーの設定 -->
        <meta-data android:name="smallIconColor" android:value="@color/jipriMainColor"/>

        <!-- 通知エリアにプッシュ通知を複数表示する設定　0:最新のみ表示 , 1:複数表示 -->
        <meta-data android:name="notificationOverlap" android:value="0"/>

        <!-- カスタムダイアログプッシュを利用する場合のみ背景画像の設定 -->
        <meta-data android:name="dialogPushBackgroundImage" android:resource="@drawable/common_google_signin_btn_icon_light"/>

        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListActivity"
            android:label="@string/title_activity_event_list"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".eActivity"
            android:label="@string/title_activity_e"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".EventDetailActivity"
            android:label="@string/title_activity_event_detail"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ParticipantsListActivity" />
        <activity android:name=".EntryActivity" />
        <activity android:name=".EventCreateActivity" />
        <activity android:name=".EventEditActivity" />
        <activity android:name=".NoticeCreateActivity" />
        <activity android:name=".NoticeDetailActivity" />
        <activity android:name=".NoticeEditActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"></activity>

        <receiver
            android:name="com.google.android.gms.gcm.GcmReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE"/>
                <category android:name="com.example.taross.jinkawa_android"/>
            </intent-filter>
        </receiver>

        <service
            android:name="com.nifty.cloud.mb.core.NCMBGcmListenerService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE"/>
            </intent-filter>
        </service>
    </application>

</manifest>